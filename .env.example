##############################################
# stock-analysis-platform / Stock Analysis Platform (.env)
# Minimal example for local development
##############################################

# ---------------------
# Backend (FastAPI)
# ---------------------
# App
APP_NAME=stock-analysis-platform
ENV=dev
LOG_LEVEL=INFO

# Database
DATABASE_URL=sqlite:///stock-analysis-platform.db

# LLM provider (mock requires no keys; use openrouter for real analysis)
LLM_PROVIDER=mock
# LLM_PROVIDER=openrouter
# OPENROUTER_API_KEY=sk-or-v1-your-key-here

# LLM model used for both analysis and chat
# Choose a model available on OpenRouter, e.g.:
# - openai/gpt-4o-mini
# - google/gemini-1.5-flash
# - anthropic/claude-3.5-haiku
LLM_MODEL=openai/gpt-4o-mini

# CORS (comma-separated origins allowed to access API)
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Optional backend tweaks
# DEFAULT_STOCK_PERIOD=2y
# DEFAULT_FORECAST_DAYS=30

# ---------------------
# Frontend (Next.js)
# ---------------------
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_WS_BASE_URL=ws://localhost:8000

# Clear all database tables on API startup (cache & chat)
# WARNING: This deletes all stored sessions, messages, and caches each run
CLEAR_DB_ON_STARTUP=true
